import RouteKey from '../types/route-key.type'

const ROUTE_CONFIG: Record<RouteKey, string> = {
  HOME: '/',
  LOGIN: '/login',
  REGISTER: '/register',
  NOTES: '/notes'
}

export { ROUTE_CONFIG }
